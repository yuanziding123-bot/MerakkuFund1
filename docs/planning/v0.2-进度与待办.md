# AIHF v0.2 — 进度与待办

> 跟踪 `AIHF产品PRD与技术架构v0.2.md` 的 M0 落地进度。
> 起点: 2026-06-22 · 工作目录 `C:\MerakkuFund` · venv `C:\polymarket\.venv`
> 原则: **不重写,给现有 LangGraph 四层引擎包一层「对象 + 评估账本 + 晋升门」**(PRD 附录 C)。
> 进度记录在文末「变更日志」。

---

## 当前状态一览

| 模块 | PRD 章节 | 状态 | 文件 |
|---|---|---|---|
| 对象层 + 状态机 | 八-A / 三 | ✅ **完成** | `polyagents/objects.py` |
| 对象层测试 | — | ✅ **完成**(7 测试) | `tests/test_objects.py` |
| 主/副 agent 框架 + agent loop | 八-B(扩展) | ✅ **完成**(14 测试) | `polyagents/orchestration/` |
| LLM Router(Claude 智能调度) | — | ✅ **完成** | `orchestration/llm_router.py` |
| Strategy 接进 web(工具 + SSE 实时 + 新标签) | 七 | ✅ **完成** | `web/agent.py` · `server.py` · `static/index.html` |
| 对象/晋升落库 | 九 | ✅ **完成**(6 测试) | `polyagents/storage/objects_store.py` |
| Objects 视图接真实数据(创建/晋升/lineage/三门) | 三 / 五 | ✅ **完成** | `web/server.py` · `static/index.html` |
| Alpha 测试(bootstrap CI Brier skill vs market) | 八-C | ✅ **完成**(6 测试) | `polyagents/evaluation/alpha.py` |
| Lab = alpha 测试(Run alpha test → EvalSummary → gate2) | 七 / 五 | ✅ **完成** | `web/server.py` `/api/objects/alpha_test` + Lab UI |
| 历史回放(PIT 价格回放喂 alpha 测试) | 八-D | ✅ **完成**(7 测试) | `polyagents/lab/backtest.py` |
| web 信息架构重组(rail: modes + library) | 七 | ✅ **完成** | `static/index.html` |
| Ask 高保真(hero + composer + 模型选择) | 七 | ✅ **完成**(4 测试) | `web/agent.py` · `static/index.html` |
| Ask Upload files MVP(拖拽 文本/图片/PDF) | 七(扩展) | ✅ **完成**(10 测试) | `polyagents/web/uploads.py` |
| Ask 只读边界(去掉写工具) | 二 / 八-B | ✅ **完成** | `web/agent.py`(`WRITE_TOOLS`) |
| Ask→Hypothesis 涌现(propose→Promote=gate1) | 三 / 五 | ✅ **完成** | `web/agent.py`(`propose_hypothesis`)+ UI |
| AgentSession 模式化(mode→工具/权限/审计) | 八-B | ✅ **完成**(7 测试) | `polyagents/runtime/session.py` |
| 审计落库(audit_events) | 九 | ✅ **完成** | `polyagents/storage/audit_store.py` + `/api/audit` |
| 评估账本表落库(forecasts/evaluations 表) | 九 | ⬜ 待办 | `polyagents/evaluation/ledger.py` |
| 评估引用带 CI(Ask 答复) #5 | 六 | ⬜ 待办 | `mcp_server.evaluation_report` |
| 对象 @ 引用(Ask 里引已有对象) #6 | 三 | ⬜ 待办 | `web/` |
| PIT 不变量测试套件 | 九 | ⬜ 待办(M1) | `tests/test_pit.py` |

测试基线: **172 passed**。git:Ask 工作已 FF 并入 `main`(不再用 feature 分支)。端到端冒烟:策略 research/full/LLM-routed 跑通;Objects 创建→晋升→lineage;Lab 历史回放 alpha(真实已结算市场);Ask 上传文件 agent 真读到;Ask 涌现 Hypothesis→Promote;一次对话审计 start→tool.call→end 落库。

---

## 已完成

### ✅ 对象层脊柱 — `polyagents/objects.py`
- 5 个 frozen 对象 `Market / Hypothesis / Strategy / Position / Portfolio`,共享 `FO` 契约(id / version / snapshot_id / state / owner / lineage / eval_summary)。
- 状态机 `draft → lab → paper → live → archived`;`archived` 唯一终态,**无后向边**。
- `make()` 造对象(v1,空 lineage)、`revise()` 改参数=升 version、`promote()` **单向/版本化/带证据**(追加 `PromotionEvent`,非法边抛 `IllegalTransition`)。
- 确定性晋升门(**非 LLM**):`eval_gate_passed`(门2:CI 下界>0 且 n≥30 且 ECE<阈值)、`risk_gate_passed`(门3:风控阈值 **且** 人工批准,永不自动开实盘)。
- `EvalSummary` 数据契约(n / brier_delta / brier_delta_ci / ece / beats_market / sample_adequate)。
- 零新依赖,不碰现有引擎。

---

## 待办(按依赖排序)

### ✅ #1 对象落库 — `storage/objects_store.py`(完成)
- `objects` 单表 JSON 列(`payload_json`)+ `promotion_events` 审计表;`ObjectStore.save/get/list/promote/promotions/counts`。
- `objects.py` 加 `to_dict`/`from_dict`(tuple 还原)/`next_states`。
- web 三端点:`GET /api/objects`、`POST /api/objects`(造 Hypothesis)、`POST /api/objects/promote`。
- Objects 标签:状态机图 + 三门图例 + 新建假设表单 + 真实对象表 + 选中看 lineage/门状态/晋升按钮。
- 仍待:`forecasts`/`evaluations` 表(随 #2 一起)。

### 🟡 #2 评估账本 — `evaluation/alpha.py`(CI 部分完成,表落库待办)
- ✅ **bootstrap CI 已做**:`alpha.py` 对某 slice 已结算预测评 Brier(`market − model`,**正=模型更好**)+ 1000 次重采样 CI → `EvalSummary`(`beats_market` = CI **下界>0**,即整段 CI 在 0 之上;符号约定已随上游 `765c91f` 统一)。
- ✅ 接进 Lab(`/api/objects/alpha_test` + Run alpha test 按钮),`EvalSummary` 挂回 Hypothesis、驱动 gate2。
- ⬜ **仍待**:专用 `forecasts` / `evaluations` 表持久化每条预测(现在评估结果挂在对象 `eval_summary`,没有逐条 forecast 落库)。

### ✅ #3 Lab 历史回放 — `lab/backtest.py`(完成)
- `BacktestRunner` 对**已结算市场**(`list_resolved_markets`)用 `interval=max` 价格历史、**按比例取 prediction_time**(只用更早 candle,PIT 已断言无泄漏)、跑确定性 signal(momentum/naive)→ `alpha_test` 评 vs 市场基线 + CI。
- 接 `/api/backtest_replay`(线程跑)+ Lab「Historical backtest」面板。`tests/test_backtest.py` 7 测试。
- 注:历史 orderbook/news 无法 PIT 重建,故只测**价格类 signal**,不冒充 LLM 假设的 edge(forward 决策日志走 `/api/objects/alpha_test`)。

### ✅ #4 Web 接一道门(Ask → Hypothesis)(完成)
- Ask agent 调**只读** `propose_hypothesis` 工具 → 聊天涌现 Hypothesis artifact 卡 → 用户点 **Promote to Lab**(= gate 1)→ `POST /api/objects` 真建对象。
- live 实测:agent 真的发 `propose_hypothesis`,args 带 statement/category/features/success。

### ✅ #5 AgentSession 薄层 — `runtime/session.py`(完成)
- `mode` 字段决定:工具子集(`readonly`)+ `PermissionPolicy`(ask 不可交易/lab 纸面/live 实盘)+ 审计强度。
- `/api/chat` 跑成 `AgentSession("ask")`;Ask 只读(去掉 `WRITE_TOOLS`)+ 审计(start/tool.call/end)。`tests/test_runtime_audit.py` 7 测试。

### ⬜ #6 PIT 不变量测试套件 — `tests/test_pit.py`(M1)
- `assert_point_in_time()` 断言函数,signal 节点入口强制调用。
- 人工构造未来信息 → 断言被拒,进 CI。
- 这是 M1 的 kill criteria(若现有数据已大面积泄漏,停后续回去修采集)。

---

## 现实校准 / 风险

- **PIT 地基是否漏水**:#3 历史回放严格 PIT,取决于现有 SQLite(`candles` / `trades` / `collections`)**有没有 `available_at` 时间戳**。若没有,门5 的「构造未来信息被拒」测试会暴露采集层就在泄漏(M1 kill criteria)。**建议在写 #2/#3 前先花十分钟查现有表够不够支撑 PIT 回放。**
- **样本量**:加密类/小众类已结算市场 n 偏小,bootstrap CI 会很宽 → 门2 大概率「方向正向但样本不足」。这是真实信号,不是 bug,如实报告。
- **两周 ~1200 行**:对象层 + 落库 + ledger 扎实;backtest 的工作量随 PIT 数据完备度浮动。

---

## kill criteria(来自 PRD 附录 B,提前记住)

| 阶段 | 停止条件 |
|---|---|
| M0 | 2 周内连 Ask→Hypothesis 闭环都跑不通 → 对象模型设计有误,回设计 |
| M1 | PIT 测试发现现有数据大面积泄漏 → 停后续,回去修采集 |
| M2 | 所有校准器在 holdout 上都跑输 market baseline → **承认当前 LLM signal 没 edge**,回 prompt/特征,不硬上 M3 |

---

## 决策记录

- pi.dev **不作地基**,仅作可选 Ask chat 外壳(MCP client);MVP 不依赖 pi。地基是 LangGraph + Anthropic SDK。
- 对象存储用 **SQLite 单表 JSON**(非分表),量大再拆。
- mentor 文件已整理进 `docs/`(product/prototypes/planning);标准 PRD = `docs/product/AIHF产品PRD与技术架构v0.2.md`。
- **Ask 模块流程**:PRD → 高保真 → 测试用例 → 实现(`docs/product/ask-module-*.md` + `docs/design/ask-module-hifi.html`)。
- **git 流程(2026-06-25 起)**:直接在 `main` 上 commit(不再开 feature 分支),本地 `pytest` 全绿后由用户自行 push;助手不 push。

---

## 变更日志

- **2026-06-22** — 建对象层脊柱 `polyagents/objects.py` + `tests/test_objects.py`(7 测试);全量 **115 passed**。建本进度文档。
- **2026-06-22** — 建主/副 agent 框架 `polyagents/orchestration/`(Supervisor agent loop + Router + Blackboard + DataAgent/SignalAgent/RiskAgent/ExecutionAgent)+ `tests/test_orchestration.py`(11 测试);全量 **126 passed**。web Skills 改成按 category 分区的独立模块。
- **2026-06-23** — 串通框架:(B)`LLMRouter` Claude 智能调度下一个副 agent + `router` 参数;(A)`run_strategy` 包成 chat 工具绑给主 agent + `/api/strategy` SSE 端点(`on_event`→SSE 桥接)+ web 新增 **Strategy 标签**(实时看 data→signal→risk 逐个跑 + 决策卡)。端到端冒烟:research/full/LLM-routed 三种都对真实市场跑通(FIFA 市场正确 HOLD)。全量 **129 passed**。
- **2026-06-23** — 对象层接真实数据(原型 A):`storage/objects_store.py`(objects + promotion_events 表)+ `objects.py` 序列化 + 3 个 web 端点 + **Objects 标签**(状态机/三门/新建假设/晋升/lineage,全真实落 SQLite)。端到端:创建 H→晋升 draft→lab→lineage 留痕跑通。全量 **135 passed**。
- **2026-06-23** — web 信息架构按原型重组:左侧 rail = **modes(Ask/Lab/Live)+ library(Objects/Markets/Evaluation)**;chat history + skills 收进 Ask 上下文;Lab 内含 Strategy/Backtest 子导航;新增 Live(gated + MCP 状态)与 Evaluation 视图(`/api/evaluation`,真实 Brier/ECE 报告)。顶栏改面包屑。
- **2026-06-23** — chat history 做成原型式独立中间列(`#askList`:搜索 + Today 分组卡片,会话可累积/载回);中间列随模式切换(Ask=会话 / Lab=假设)。修全局 `button{background:transparent}`(修 nav/labtab 发白)。
- **2026-06-23** — Lab 视图按原型重建(假设为中心):Lab 子导航加 **Hypotheses**(默认)= 晋升门流(真实门状态)+ 指标卡 + 选中假设详情/回测面板 + 校准曲线(占位)+ lineage + 全假设表,全部由 `/api/objects?type=hypothesis` 真实驱动;回测/校准数字留诚实占位(待 #2/#3),不编数。Strategy/Backtest 保留为子标签。
- **2026-06-23** — mentor 定调「Lab 主要做 alpha 测试」。建 `evaluation/alpha.py`:`alpha_test()` 对某 slice 的已结算预测评 Brier(model vs market)+ **bootstrap CI**,产出 `EvalSummary`(`beats_market`=CI 下界>0),`tests/test_alpha.py` 6 测试。接 `/api/objects/alpha_test` 端点(挂 EvalSummary 落库)+ Lab **▶ Run alpha test** 按钮 + BEATS/NO-EDGE 判定(positive brier_delta = market-model),跑完 gate2 自动评估。全量 **141 passed**。
- **2026-06-23** — #3 历史回放 `lab/backtest.py`:`BacktestRunner` 对**已结算市场**用 `interval=max` 价格历史、**按比例取 prediction_time**(只用更早 candle,PIT 已断言)、跑确定性 signal(momentum/naive)、用 `alpha_test` 评 vs 市场基线 + bootstrap CI。client 加 `list_resolved_markets`。接 `/api/backtest_replay`(线程跑)+ Lab「Historical backtest」面板(signal/predict-at/max + BEATS/NO-EDGE)。`tests/test_backtest.py` 7 测试。**真实跑通**:20 个已结算市场 momentum@50% → NO EDGE(brier 0.092 vs 0.092,CI 含 0,诚实)。全量 **148 passed**。**alpha 测试从此有真实数据可吃。**
- **2026-06-24** — Ask 模块按流程交付(PRD→高保真→测试→实现):`docs/product/ask-module-PRD.md` + `docs/design/ask-module-hifi.html`(深色)。实现 **Ask hero 空状态 + composer**(＋菜单/Chat 胶囊/**模型选择 Sonnet·Opus·Haiku**→ `/api/chat` body.model/附件/发送);后端 `ASK_MODELS` + `resolve_model`。`tests/test_ask_module.py`。全量 **152 passed**。
- **2026-06-24** — Ask **Upload files MVP**(`uploads.py`):拖拽 文本/图片/PDF 进输入框 → `extract`(文本/pypdf/base64)→ 进程内缓存 → `/api/upload` + `/api/chat` `attachments[]` 注入(文本块/多模态图片块)。装 `pypdf`+`python-multipart`。`tests/test_uploads.py` 10。端到端实测:agent 真读到上传内容。全量 **162 passed**。
- **2026-06-25** — #1 Ask **只读边界**:`build_tools(readonly=True)` 去掉 `WRITE_TOOLS`(run_trading_strategy/size_position/paper_execute/settle_markets),`build_agent` 默认只读。#2 **涌现 Hypothesis**:只读 `propose_hypothesis` 工具 + Ask 纪律 preamble → 聊天浮出卡片 → Promote=gate1。全量 **165 passed**。
- **2026-06-25** — #3 审计 + #4 AgentSession:`runtime/session.py`(`AgentSession`/`PermissionPolicy`,mode→工具/权限/审计)+ `storage/audit_store.py`(`audit_events` 表)。`/api/chat` 跑成 `AgentSession("ask")`,落 session.start/tool.call/end + object.create(gate1)+ promotion;`/api/audit` + Live 视图 Audit 面板。`tests/test_runtime_audit.py` 7。全量 **172 passed**。
- **2026-06-25** — git 流程切换:`feature/ask-module`(8 commit)**FF 并入 main**、删分支(本地+远端);以后直接在 main 干活,用户自行 push。
- **2026-07-14** — Lab historical ingestion 增强:Polymarket 多 outcome/range market 拆成 token-level collections;默认 `prediction_policy=multi` 生成 25%/50%/75% 三个 PIT snapshots;report 增加 `edge_evidence` 区分 research edge 与 executable edge availability;historical news 增加 run-level cache。默认 DataStore 当前 `collections=54`、`multi_snapshot_collections=18`;真实小样本 `fetched_markets=3 → inserted=12 + updated=6`;相关回归 **36 passed**。详见 `docs/product/lab-edge-coverage-progress-2026-07-14.md`。
